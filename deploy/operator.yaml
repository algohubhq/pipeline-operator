apiVersion: apps/v1
kind: Deployment
metadata:
  name: algorun-endpoint-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: algorun-endpoint-operator
  template:
    metadata:
      labels:
        name: algorun-endpoint-operator
    spec:
      serviceAccountName: algorun-endpoint-operator
      containers:
        - name: endpoint-operator
          # Replace this with the built image name
          image: algohub/endpoint-operator:1aea7fe6d552b51cc72e43f87701202665e71e8b
          command:
          - endpoint-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "endpoint-operator"
