apiVersion: apps/v1
kind: Deployment
metadata:
  name: algorun-endpoint-operator
  labels:
    system: algorun
    tier: backend
    app: endpoint-operator
    env: production
spec:
  replicas: 1
  selector:
    matchLabels:
      name: algorun-endpoint-operator
  template:
    metadata:
      labels:
        name: algorun-endpoint-operator
    spec:
      serviceAccountName: algorun-endpoint-operator
      containers:
        - name: endpoint-operator
          # Replace this with the built image name
          image: algohub/endpoint-operator:924d31ec8cf121c91410d89ddaab66395fc2f2b4
          command:
          - endpoint-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "algorun-endpoint-operator"
