apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: kafkaconnects.kafka.strimzi.io
spec:
  group: kafka.strimzi.io
  names:
    kind: KafkaConnect
    listKind: KafkaConnectList
    plural: kafkaconnects
    singular: kafkaconnect
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: KafkaConnect is the Schema for the KafkaConnect API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: KafkaConnectSpec defines the desired state of KafkaConnect
          properties:
            authentication:
              description: KafkaClientAuthentication defines the desired state of
                KafkaClientAuthentication
              properties:
                accessToken:
                  description: GenericSecretSource defines the desired state of GenericSecretSource
                  properties:
                    key:
                      type: string
                    secretName:
                      type: string
                  type: object
                accessTokenIsJwt:
                  type: boolean
                certificateAndKey:
                  description: CertAndKeySecretSource defines the desired state of
                    CertAndKeySecretSource
                  properties:
                    key:
                      type: string
                  type: object
                clientId:
                  type: string
                clientSecret:
                  description: GenericSecretSource defines the desired state of GenericSecretSource
                  properties:
                    key:
                      type: string
                    secretName:
                      type: string
                  type: object
                disableTlsHostnameVerification:
                  type: boolean
                maxTokenExpirySeconds:
                  type: integer
                passwordSecret:
                  description: PasswordSecretSource defines the desired state of PasswordSecretSource
                  properties:
                    password:
                      type: string
                    secretName:
                      type: string
                  type: object
                refreshToken:
                  description: GenericSecretSource defines the desired state of GenericSecretSource
                  properties:
                    key:
                      type: string
                    secretName:
                      type: string
                  type: object
                tlsTrustedCertificates:
                  items:
                    description: CertSecretSource defines the desired state of CertSecretSource
                    properties:
                      certificate:
                        type: string
                      secretName:
                        type: string
                    type: object
                  type: array
                tokenEndpointUri:
                  type: string
                type:
                  description: KafkaClientAuthenticationTypes
                  type: string
                username:
                  type: string
              type: object
            bootstrapServers:
              type: string
            config:
              additionalProperties:
                type: string
              type: object
            image:
              type: string
            jvmOptions:
              description: JvmOptions defines the desired state of JvmOptions
              properties:
                -XX:
                  additionalProperties:
                    type: string
                  type: object
                -Xms:
                  type: string
                -Xmx:
                  type: string
                gcLoggingEnabled:
                  type: boolean
                javaSystemProperties:
                  items:
                    description: NameValue defines the desired state of NameValue
                    properties:
                      name:
                        type: string
                      value:
                        type: string
                    type: object
                  type: array
              type: object
            livenessProbe:
              description: Probe describes a health check to be performed against
                a container to determine whether it is alive or ready to receive traffic.
              properties:
                exec:
                  description: One and only one of the following should be specified.
                    Exec specifies the action to take.
                  properties:
                    command:
                      description: Command is the command line to execute inside the
                        container, the working directory for the command  is root
                        ('/') in the container's filesystem. The command is simply
                        exec'd, it is not run inside a shell, so traditional shell
                        instructions ('|', etc) won't work. To use a shell, you need
                        to explicitly call out to that shell. Exit status of 0 is
                        treated as live/healthy and non-zero is unhealthy.
                      items:
                        type: string
                      type: array
                  type: object
                failureThreshold:
                  description: Minimum consecutive failures for the probe to be considered
                    failed after having succeeded. Defaults to 3. Minimum value is
                    1.
                  format: int32
                  type: integer
                httpGet:
                  description: HTTPGet specifies the http request to perform.
                  properties:
                    host:
                      description: Host name to connect to, defaults to the pod IP.
                        You probably want to set "Host" in httpHeaders instead.
                      type: string
                    httpHeaders:
                      description: Custom headers to set in the request. HTTP allows
                        repeated headers.
                      items:
                        description: HTTPHeader describes a custom header to be used
                          in HTTP probes
                        properties:
                          name:
                            description: The header field name
                            type: string
                          value:
                            description: The header field value
                            type: string
                        required:
                        - name
                        - value
                        type: object
                      type: array
                    path:
                      description: Path to access on the HTTP server.
                      type: string
                    port:
                      anyOf:
                      - type: integer
                      - type: string
                      description: Name or number of the port to access on the container.
                        Number must be in the range 1 to 65535. Name must be an IANA_SVC_NAME.
                      x-kubernetes-int-or-string: true
                    scheme:
                      description: Scheme to use for connecting to the host. Defaults
                        to HTTP.
                      type: string
                  required:
                  - port
                  type: object
                initialDelaySeconds:
                  description: 'Number of seconds after the container has started
                    before liveness probes are initiated. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes'
                  format: int32
                  type: integer
                periodSeconds:
                  description: How often (in seconds) to perform the probe. Default
                    to 10 seconds. Minimum value is 1.
                  format: int32
                  type: integer
                successThreshold:
                  description: Minimum consecutive successes for the probe to be considered
                    successful after having failed. Defaults to 1. Must be 1 for liveness
                    and startup. Minimum value is 1.
                  format: int32
                  type: integer
                tcpSocket:
                  description: 'TCPSocket specifies an action involving a TCP port.
                    TCP hooks not yet supported TODO: implement a realistic TCP lifecycle
                    hook'
                  properties:
                    host:
                      description: 'Optional: Host name to connect to, defaults to
                        the pod IP.'
                      type: string
                    port:
                      anyOf:
                      - type: integer
                      - type: string
                      description: Number or name of the port to access on the container.
                        Number must be in the range 1 to 65535. Name must be an IANA_SVC_NAME.
                      x-kubernetes-int-or-string: true
                  required:
                  - port
                  type: object
                timeoutSeconds:
                  description: 'Number of seconds after which the probe times out.
                    Defaults to 1 second. Minimum value is 1. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes'
                  format: int32
                  type: integer
              type: object
            metrics:
              description: JMXExporter defines the desired state of JMXExporter
              properties:
                blacklistObjectNames:
                  items:
                    type: string
                  type: array
                hostPort:
                  type: string
                jmxUrl:
                  type: string
                lowercaseOutputLabelNames:
                  type: boolean
                lowercaseOutputName:
                  type: boolean
                password:
                  type: string
                rules:
                  items:
                    description: JMXExporterRule defines the desired state of JMXExporterRule
                    properties:
                      help:
                        type: string
                      labels:
                        additionalProperties:
                          type: string
                        type: object
                      name:
                        type: string
                      pattern:
                        type: string
                      type:
                        type: string
                      value:
                        type: string
                      valueFactor:
                        type: string
                    type: object
                  type: array
                ssl:
                  type: boolean
                startDelaySeconds:
                  type: integer
                username:
                  type: string
                whitelistObjectNames:
                  items:
                    type: string
                  type: array
              type: object
            readinessProbe:
              description: Probe describes a health check to be performed against
                a container to determine whether it is alive or ready to receive traffic.
              properties:
                exec:
                  description: One and only one of the following should be specified.
                    Exec specifies the action to take.
                  properties:
                    command:
                      description: Command is the command line to execute inside the
                        container, the working directory for the command  is root
                        ('/') in the container's filesystem. The command is simply
                        exec'd, it is not run inside a shell, so traditional shell
                        instructions ('|', etc) won't work. To use a shell, you need
                        to explicitly call out to that shell. Exit status of 0 is
                        treated as live/healthy and non-zero is unhealthy.
                      items:
                        type: string
                      type: array
                  type: object
                failureThreshold:
                  description: Minimum consecutive failures for the probe to be considered
                    failed after having succeeded. Defaults to 3. Minimum value is
                    1.
                  format: int32
                  type: integer
                httpGet:
                  description: HTTPGet specifies the http request to perform.
                  properties:
                    host:
                      description: Host name to connect to, defaults to the pod IP.
                        You probably want to set "Host" in httpHeaders instead.
                      type: string
                    httpHeaders:
                      description: Custom headers to set in the request. HTTP allows
                        repeated headers.
                      items:
                        description: HTTPHeader describes a custom header to be used
                          in HTTP probes
                        properties:
                          name:
                            description: The header field name
                            type: string
                          value:
                            description: The header field value
                            type: string
                        required:
                        - name
                        - value
                        type: object
                      type: array
                    path:
                      description: Path to access on the HTTP server.
                      type: string
                    port:
                      anyOf:
                      - type: integer
                      - type: string
                      description: Name or number of the port to access on the container.
                        Number must be in the range 1 to 65535. Name must be an IANA_SVC_NAME.
                      x-kubernetes-int-or-string: true
                    scheme:
                      description: Scheme to use for connecting to the host. Defaults
                        to HTTP.
                      type: string
                  required:
                  - port
                  type: object
                initialDelaySeconds:
                  description: 'Number of seconds after the container has started
                    before liveness probes are initiated. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes'
                  format: int32
                  type: integer
                periodSeconds:
                  description: How often (in seconds) to perform the probe. Default
                    to 10 seconds. Minimum value is 1.
                  format: int32
                  type: integer
                successThreshold:
                  description: Minimum consecutive successes for the probe to be considered
                    successful after having failed. Defaults to 1. Must be 1 for liveness
                    and startup. Minimum value is 1.
                  format: int32
                  type: integer
                tcpSocket:
                  description: 'TCPSocket specifies an action involving a TCP port.
                    TCP hooks not yet supported TODO: implement a realistic TCP lifecycle
                    hook'
                  properties:
                    host:
                      description: 'Optional: Host name to connect to, defaults to
                        the pod IP.'
                      type: string
                    port:
                      anyOf:
                      - type: integer
                      - type: string
                      description: Number or name of the port to access on the container.
                        Number must be in the range 1 to 65535. Name must be an IANA_SVC_NAME.
                      x-kubernetes-int-or-string: true
                  required:
                  - port
                  type: object
                timeoutSeconds:
                  description: 'Number of seconds after which the probe times out.
                    Defaults to 1 second. Minimum value is 1. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes'
                  format: int32
                  type: integer
              type: object
            replicas:
              type: integer
            resources:
              description: ResourceRequirements describes the compute resource requirements.
              properties:
                limits:
                  additionalProperties:
                    type: string
                  description: 'Limits describes the maximum amount of compute resources
                    allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                  type: object
                requests:
                  additionalProperties:
                    type: string
                  description: 'Requests describes the minimum amount of compute resources
                    required. If Requests is omitted for a container, it defaults
                    to Limits if that is explicitly specified, otherwise to an implementation-defined
                    value. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                  type: object
              type: object
            tls:
              description: KafkaConnectTLS defines the desired state of KafkaConnectTls
              properties:
                trustedCertificates:
                  items:
                    description: CertSecretSource defines the desired state of CertSecretSource
                    properties:
                      certificate:
                        type: string
                      secretName:
                        type: string
                    type: object
                  type: array
              type: object
            version:
              type: string
          type: object
        status:
          description: KafkaConnectStatus defines the observed state of KafkaTopic
          properties:
            conditions:
              items:
                description: Condition defines the desired state of Condition
                properties:
                  lastTransitionTime:
                    type: string
                  message:
                    type: string
                  reason:
                    type: string
                  status:
                    type: string
                  type:
                    type: string
                type: object
              type: array
            connectorPlugins:
              items:
                description: ConnectorPlugin defines the observed state of ConnectorPlugin
                properties:
                  class:
                    type: string
                  type:
                    type: string
                  version:
                    type: string
                type: object
              type: array
            observedGeneration:
              type: integer
            url:
              type: string
          type: object
      type: object
  version: v1beta1
  versions:
  - name: v1beta1
    served: true
    storage: true
